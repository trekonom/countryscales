% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label-euro-de.R
\name{label_euro_de}
\alias{label_euro_de}
\alias{euro_de}
\alias{label_dollar_de}
\alias{dollar_de}
\title{Label currencies (e.g. 2,5€, 50€ or $5,3, $100)}
\usage{
label_euro_de(
  accuracy = 1,
  scale = 1,
  big.mark = ".",
  decimal.mark = ",",
  prefix = "",
  suffix = "€",
  trim = TRUE,
  ...
)

euro_de(
  x,
  accuracy = 1,
  scale = 1,
  big.mark = ".",
  decimal.mark = ",",
  prefix = "",
  suffix = "€",
  trim = TRUE,
  ...
)

label_dollar_de(
  accuracy = 1,
  scale = 1,
  big.mark = ".",
  decimal.mark = ",",
  prefix = "$",
  suffix = "",
  trim = TRUE,
  ...
)

dollar_de(
  x,
  accuracy = 1,
  scale = 1,
  big.mark = ".",
  decimal.mark = ",",
  prefix = "$",
  suffix = "",
  trim = TRUE,
  ...
)
}
\arguments{
\item{accuracy}{Number to round to. If \code{NULL}, the default,
values will be rounded to the nearest integer, unless any of the
values has non-zero fractional component (e.g. cents) and the largest
value is less than \code{largest_with_cents} which by default is 100,000.}

\item{scale}{A scaling factor: \code{x} will be multiplied by \code{scale} before
formating. This is useful if the underlying data is very small or very
large.}

\item{big.mark}{Character used between every 3 digits to separate thousands.}

\item{decimal.mark}{The character to be used to indicate the numeric
decimal point.}

\item{prefix}{Symbols to display before and after value.}

\item{suffix}{Symbols to display before and after value.}

\item{trim}{Logical, if \code{FALSE}, values are right-justified to a common
width (see \code{\link[base:format]{base::format()}}).}

\item{...}{additional arguments passed to \code{\link[scales]{label_dollar}}, etc.}

\item{x}{A numeric vector}
}
\description{
Format numbers as currencies
}
\details{
German wrapper around scales::label_dollar with default \code{big.mark = "."} and
\code{decimal.mark = ","}. For \code{euro_de} the prefix defaults to ""
and the suffix to the euro sign, for \code{dollar_de} the prefix defaults
to "$" and the suffix to "".
}
\examples{
# ex-label_euro_de.R
set.seed(42)
x <- 100 * runif(10)
# Formatting numbers as currencies
euro_de(x)
dollar_de(x, accuracy = .1)
# For other currencies or if you want to switch the position
# of the currency symbol switch the suffix and/or prefix
# Dollar symbol after number
euro_de(x, suffix = "$")
# British pound before number
dollar_de(x, prefix = "\u00a3")
# For use as labeller e.g. with ggplot2
library(ggplot2)
p <- ggplot(gapminder15, aes(gdp_per_cap, life_exp)) +
  geom_point()
# Dollar prefix
p +
  scale_x_continuous(labels = label_dollar_de())
# Euro suffix
p +
  scale_x_continuous(labels = label_euro_de())
}
\seealso{
\code{\link[scales]{label_dollar}}
}
